{
  "name": "gateway-quickstart",
  "version": "1.0.0",
  "private": true,
  "description": "Protect any MCP server with @bolyra/gateway in under 2 minutes",
  "scripts": {
    "server": "node server.js",
    "gateway": "npx @bolyra/gateway --config gateway.yaml --dev"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@bolyra/gateway": "^0.1.0",
    "express": "^4.21.0"
  }
}
